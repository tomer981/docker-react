language: generic 
sudo: required
services:
  - docker


before_install:
  - docker build -t tomer981/docker-react -f Dockerfile.dev .

scirpt:
  - docker run -e CI=true tomer981/docker-react npm run test -- --coverage --watchAll=false   

